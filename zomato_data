 cartQuantity=0;
       
       constructor(carSer : CartService){
                carSer.getCartObservable().subscribe((newCart)=>{
                       this.cartQuantity = newCart.totalCount
                })

       }

 <a routerLink="/cart-page">Cart <span *ngIf="cartQuantity">{{ cartQuantity}}</span> </a>
